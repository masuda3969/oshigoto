<div class = "container">
  <div class = "row justify-content-center mb-3">
    <h3>本当に退会しますか・・・？</h3>
  </div>
  <div class = "row justify-content-center mb-5">
    <p>退会するとサイトの閲覧が出来なくなります</br>
    また、推しの布教をしてきた投稿も消されてしまいます。</p>
  </div>
  <div class = "row justify-content-around mb-4">
    <%= link_to "退会する",withdrawal_path(current_user.id), method: :patch , data: { confirm: '本当に退会しますか？' }, class: "btn btn-danger btn-sm" %>
    <%= link_to "退会しない", user_path(current_user.id), class: "btn btn-primary btn-sm" %>
  </div>
</div>